export interface UiState {
  isDarkTheme: boolean;
  // isLoadingDarkTheme: boolean;
  // error: string | null;
}

export const initialUiState: UiState = {
  isDarkTheme: false,
  // isLoadingDarkTheme: false,
  // error: null,
};
